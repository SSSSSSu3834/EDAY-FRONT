import imageSrc from '../../assets/_common/tip_icon.svg';

const Tip = () => {
    return (
        <div className='Tip'>
            <img src={imageSrc} />
        </div>
    );
};

export default Tip;
